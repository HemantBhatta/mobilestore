import React from 'react'

const Title = ({title}) => {
    return (
        <div className='title-section'>
            <p>{title}</p>
            <div/>
        </div>
    )
}

export default Title
